# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=password
DB_DATABASE=techblog

# Session Configuration
SESSION_SECRET=your_session_secret_here

# Port (optional, if not specified, the default port used is 3000)
PORT=3000